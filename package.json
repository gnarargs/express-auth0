{
  "name": "js-starter",
  "version": "0.0.1",
  "description": "Starter template for JavaScript apps",
  "main": "index.js",
  "scripts": {
    "clean": "rm -r build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
